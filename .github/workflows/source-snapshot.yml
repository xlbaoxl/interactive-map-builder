name: source snapshot

on:
  push:
    branches:
      - agent/v0.3.2-controls-and-updates
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Upload source checkout
        uses: actions/upload-artifact@v4
        with:
          name: interactive-map-builder-source
          path: .
          include-hidden-files: true
          retention-days: 1
